from .app import create_app

APP = create_app()
#  this says to invoke the function in app.py to start
# the app up
# this if the first file seen

# now we can start app from the outer folder
# export FLASK_APP=twitoff

# in terminal make .env inside outer folder
#Then go to .env
